{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {{/* Standard Hugo pages */}}
  {{ range .Pages }}
  {{- if not (or (eq .Kind "taxonomy") (eq .Kind "term")) }}
  <url>
    <loc>{{ .Permalink }}</loc>{{ if not .Lastmod.IsZero }}
    <lastmod>{{ .Lastmod.Format "2006-01-02T15:04:05-07:00" | safeHTML }}</lastmod>{{ end }}
  </url>
  {{- end }}
  {{ end }}
  {{/* Hand-built interactive apps under static/ — Hugo doesn't know about
       these, so declare them explicitly from the same data that renders
       their cards. Keeps every course/walkthrough discoverable by Google. */}}
  {{ range site.Data.courses }}
  <url><loc>{{ .url | absURL }}</loc></url>
  {{ end }}
  {{ range site.Data.pr_walkthroughs }}
  <url><loc>{{ .url | absURL }}</loc></url>
  {{ end }}
</urlset>
